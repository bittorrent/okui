#!/bin/bash -e

script_dir="$( cd "$( dirname "${BASH_SOURCE[0]}" )" && pwd )"
cd $script_dir/../..

if [[ -z $ANALYSIS ]] ; then
    ./scripts/build --target=iphoneos --install
else
    ./scripts/build --target=iphoneos --variant=debug --enable-analysis
fi
